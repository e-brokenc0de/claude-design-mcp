node_modules/
dist/
*.log
.DS_Store

# Claude Code runtime state (keep .claude/skills tracked)
.claude/scheduled_tasks.lock
.claude/**/*.lock

# Secrets & auth state — NEVER commit
.env
.env.*
!.env.example
.auth/
storageState.json
browser-profile/

# Recon artifacts (HARs can leak cookies)
recon/*.har
recon/*.json
recon/*.jsonl
!recon/.gitkeep

# Project state cache
.claude-design-mcp/
recon/*.bin
